<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ── Acción: Dashboard de Reporte de Pagos ── -->
    <record id="action_payment_report_dashboard" model="ir.actions.client">
        <field name="name">Reporte de Pagos</field>
        <field name="tag">somgroup_payment_report_dashboard</field>
        <field name="context">{}</field>
    </record>

    <!-- ── Menú ── -->
    <menuitem id="menu_payment_report_dashboard"
              name="Reporte de Pagos"
              parent="purchase.menu_purchase_root"
              action="action_payment_report_dashboard"
              sequence="26"/>

</odoo>